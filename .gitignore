# Sender credentials must never be committed -- they live only as a
# repository secret (LEGAL_MAILS_CREDENTIALS) on GitHub, or locally
# in credentials.json for a local-only run.
credentials.json
config.json

__pycache__/
*.pyc

# Excel lock file, created while Schedule.xlsx is open
~$*.xlsx
